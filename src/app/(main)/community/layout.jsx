export default function ArticlesLayout({ children }) {
  return <>{children}</>;
}

export const metadata = {
  title: "Community | BlogVerse",
  robots: "noindex, follow",
  description: "Browse all Community on BlogVerse",
};
