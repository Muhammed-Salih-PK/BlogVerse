export default function ArticlesLayout({ children }) {
  return <>{children}</>;
}

export const metadata = {
  title: "Trending | BlogVerse",
  robots: "noindex, follow",
  description: "Browse all Treding on BlogVerse",
};
