import { useDispatch, useSelector } from 'react-redux';

// Optional: you can type these with TypeScript if needed
export const useAppDispatch = () => useDispatch();
export const useAppSelector = useSelector;
